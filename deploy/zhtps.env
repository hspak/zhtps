# systemd splits ZHTPS_ARGS into command-line arguments; this is not a shell script.
# Keep admin on loopback: it is unauthenticated and always uses plain HTTP.
ZHTPS_ARGS="--address 127.0.0.1 --port 8080 --admin-address 127.0.0.1 --admin-port 9090"
