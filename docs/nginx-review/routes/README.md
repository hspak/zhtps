These preliminary runs are excluded from the nginx review's performance
conclusions. Both named variants and all six measured run records contain the
same binary SHA-256, despite different archived sources. They are not a valid
before/after routing comparison. The run summaries and source hashes are retained
for traceability; the catalog entry for `summary.json` describes the recorded
traffic only. Raw records and source archives have been removed.

> Artifact retention: [Run summaries and setup records](../../runs/README.md) are retained.
> Raw traces, binaries, profiles, and source snapshots were removed.
> Artifact paths and restoration commands below describe the original runs.

See the comparison audit and
[the source review](../../nginx-review.md). All prototype source changes were
removed after the user clarified that the task is review only.
